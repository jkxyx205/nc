<?xml version="1.0"?>
<hibernate-mapping>
 <sql-query name="teacher.list">
 	<![CDATA[
 			 SELECT id,
				name,
				sex,
				email,
				tel,
				jobType,
				workWay,
				education
			FROM nc_teacher  WHERE status = '1' AND name = :name
    ]]>
</sql-query>

 <sql-query name="teacher.deleteById">
 	<![CDATA[
 		 DELETE FROM nc_teacher WHERE id = :id
    ]]>
</sql-query>

</hibernate-mapping>

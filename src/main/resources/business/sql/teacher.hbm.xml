<?xml version="1.0"?>
<hibernate-mapping>
 <sql-query name="teacher.list">
 	<![CDATA[
 		 SELECT id,
				number,
				name,
				birthday,
				sex,
				region,
				education,
				email,
				tel,
				teach_type,
				status
			FROM nc_teacher WHERE name = :name and sex in (:sex)
    ]]>
</sql-query>

 <sql-query name="teacher.deleteById">
 	<![CDATA[
 		 DELETE FROM nc_teacher WHERE id = :id
    ]]>
</sql-query>

</hibernate-mapping>
